const responseCustomErrorsEnum = require('./response-custom-errors.enum');
const responseStatusCodesEnum = require('./response-status-codes.enum');
const tableNamesEnum = require('./table-names.enum');

module.exports = {
  responseCustomErrorsEnum,
  responseStatusCodesEnum,
  tableNamesEnum
};
