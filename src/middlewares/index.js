const groupMiddlewares = require('./group');
const lessonMiddlewares = require('./lesson');
const studentMiddlewares = require('./student');
const teacherMiddlewares = require('./teacher');

module.exports = {
  groupMiddlewares,
  lessonMiddlewares,
  studentMiddlewares,
  teacherMiddlewares
};
